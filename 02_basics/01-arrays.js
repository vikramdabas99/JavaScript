// array :- declaring multipule values in a single variable

const myArr = [1,2,3,4,5]
const heros = ['shaktiman', 'nagraaj']

const newArr = new Array(1,2,3,4,5)
// console.log(myArr[3]); // access by index

// Array methods
// myArr.push(7) // Add a value in the last index
// myArr.pop() // Remove the last index value 

// myArr.unshift(0) // Add a value in starting index
// myArr.shift() // remove index first value

// console.log(myArr.includes(2));
// console.log(myArr.indexOf(2));

// const newArray = myArr.join() // convert into a string
// console.log(newArray);

// console.log(myArr);

// slice, splice
// console.log('A ', myArr);

// const myn1 = myArr.slice(1, 3) // not incllude last value

// console.log(myn1);
// console.log('B ', myArr);

// const myn2 = myArr.splice(1, 3) // include last value also and manipulate the original array and return the remaning values
// console.log('C ', myArr);
// console.log(myn2);